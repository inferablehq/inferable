{
  "name": "@inferable/trpc-connector",
  "version": "0.1.0",
  "description": "tRPC connector for Inferable",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "tsx -r dotenv/config src/test.ts --watch src/index.ts",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "prepare": "npm run clean && npm run build"
  },
  "dependencies": {
    "@trpc/server": "^10.0.0",
    "inferable": "^0.30.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "@trpc/server": "^10.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}